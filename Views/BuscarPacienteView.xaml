<UserControl x:Class="FitodietCalc.Views.BuscarPacienteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- FORMULARIO Y EVALUACIÓN -->
        <StackPanel Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Nombre:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="TBNombre" Grid.Row="0" Grid.Column="1" Margin="5"/>

                <TextBlock Text="Primer Apellido:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="TBApellido1" Grid.Row="1" Grid.Column="1" Margin="5"/>

                <TextBlock Text="Segundo Apellido:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="TBApellido2" Grid.Row="2" Grid.Column="1" Margin="5"/>

                <TextBlock Text="Telefono:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="TBTelefono"  Grid.Row="3" Grid.Column="1" Margin="5"/>

                <TextBlock Text="Email:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="TBEmail" Grid.Row="4" Grid.Column="1" Margin="5"/>

                <TextBlock Text="Sexo:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox Grid.Row="5" Grid.Column="1" Margin="5">
                    <ComboBoxItem Content="Hombre"/>
                    <ComboBoxItem Content="Mujer"/>
                </ComboBox>

                <TextBlock Text="Fecha de Nacimiento:" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <DatePicker Grid.Row="6" Grid.Column="1" Margin="5"/>
            </Grid>

            <Expander x:Name="EvaluacionExpander" Header="Datos de Evaluación" Margin="0,20,0,10">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Peso (kg):" Grid.Row="0" Grid.Column="0" Margin="0,5"/>
                        <TextBox x:Name="TBPeso" Grid.Row="0" Grid.Column="1" Margin="5"/>
                        <TextBlock Text="Altura (cm):" Grid.Row="1" Grid.Column="0" Margin="0,5"/>
                        <TextBox x:Name="TBAltura" Grid.Row="1" Grid.Column="1" Margin="5"/>
                        <TextBlock Text="% Grasa corporal (opcional):" Grid.Row="2" Grid.Column="0" Margin="0,5"/>
                        <TextBox x:Name="TBGrasa" Grid.Row="2" Grid.Column="1" Margin="5"/>
                    </Grid>

                    <!-- NAVEGACIÓN ENTRE EVALUACIONES -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button x:Name="BtnAnteriorEval" Content="⟨ Anterior" Margin="5" Width="100"/>
                        <Button x:Name="BtnSiguienteEval" Content="Siguiente ⟩" Margin="5" Width="100"/>
                    </StackPanel>
                </StackPanel>
            </Expander>
        </StackPanel>

        <!-- BOTONES FINALES -->
        <Border Grid.Row="1" Margin="0,20,0,0" VerticalAlignment="Bottom">
            <Grid HorizontalAlignment="Right">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BtnLimpiar" Content="Limpiar Todo" Margin="5" Grid.Column="0"/>
                <Button x:Name="BtnCargar" Content="Cargar" Margin="5" Grid.Column="1"/>
                <Button x:Name="BtnModificarEval" Content="Modificar Evaluación" Margin="5" Grid.Column="2"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
